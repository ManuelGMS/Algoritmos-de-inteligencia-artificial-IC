using cFila = std::vector<char>;

using dFila = std::vector<double>;

using dMatriz = std::vector<dFila>;

using cMatriz = std::vector<cFila>;

using coordenada = std::pair<int,int>;

using coordenadas = std::vector<coordenada>;
