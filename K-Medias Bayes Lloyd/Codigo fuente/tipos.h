using nombreClase = std::string;

using vector = std::vector<double>;

using vectorMedia = std::vector<double>;

using vectorCentral = std::vector<double>;

using matriz = std::vector<std::vector<double>>;

using matrizU = std::map<std::string,std::vector<double>>;;

using conjuntoDeMuestras = std::vector<std::vector<double>>;

using matrizDeCobarianza = std::vector<std::vector<double>>;

using matrizDeCobarianzaInversa = std::vector<std::vector<double>>;

using subMatricesDeCobarianza = std::vector<std::vector<std::vector<double>>>;
